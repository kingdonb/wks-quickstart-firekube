apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: podinfo
  namespace: default
spec:
  rules:
  - host: podinfo
    http:
      paths:
      - path: /
        backend:
          serviceName: podinfo
          servicePort: 9898
